// import * as functions from "firebase-functions";
// Users
// When a user name is updated...
// 1. update the name field with a concatenation
// 2. Denormalise the data to other objects (Subscriptons, etc.) where a UserProfile type is set

// When a comment is added, increment the discussion counter
// When a comment is deleted, decrement the discussion counter

// When a document is hidden, update the hiddenBy value
// When a document is deleted, add it back, set the status to deleted and save the old version in the history sub-collection

// When a proposal is created, add a `votingEnds` field

/*
Callable functions
==================



*/